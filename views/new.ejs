<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1><%= title %></h1>
    <form method="POST" action="<%= message ? '/edit/' + messageIndex : '/new' %>">
        <div>
            <label for="username">Username:</label>
            <input type="text" id="username" name="username" value="<%= message ? message.user : '' %>" required>
        </div>
        <div>
            <label for="content">Message:</label>
            <textarea id="content" name="content" required><%= message ? message.text : '' %></textarea>
        </div>
        <div>
            <button type="submit">Submit</button>
        </div>
    </form>
</body>
</html>
